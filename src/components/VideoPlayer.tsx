'use client';
// import { useStore } from '@/lib/store';

export default function VideoPlayer() {
  // const videoUrl = useStore(s => s.videoUrl);
  // if (!videoUrl) return null;
  return (
    // <video src={videoUrl} controls className="max-w-full rounded-xl shadow" />
    <div>VideoPlayer</div>
  );
}
